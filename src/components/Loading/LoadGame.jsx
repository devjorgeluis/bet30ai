import iconLoading from '/src/assets/svg/loading.svg';

const LoadGame = () => {
    return (
        <img src={iconLoading} width={150} className="loading-icon" />
    );
};

export default LoadGame;
